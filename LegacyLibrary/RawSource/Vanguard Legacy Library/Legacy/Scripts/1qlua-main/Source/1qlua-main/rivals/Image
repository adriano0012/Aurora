local HttpService = game:GetService("HttpService")
local TweenService = game:GetService("TweenService")
local CoreGui = game:GetService("CoreGui")
local Players = game:GetService("Players")
local player = Players.LocalPlayer
local RAW_URL = "https://raw.githubusercontent.com/1Qlua-RBXscript/1qlua/refs/heads/main/1Qlua_setting/Imeages"
local imageList = {}
local function fetchGitHub()
    local requestFunc = (syn and syn.request) or (http and http.request) or http_request or request
    
    if requestFunc then
        local res = requestFunc({Url = RAW_URL, Method = "GET"})
        return res.Success and res.Body or nil
    else
        local s, r = pcall(function() return HttpService:GetAsync(RAW_URL) end)
        return s and r or nil
    end
end

local function updateImageList()
    local data = fetchGitHub()
    if data then
        local tempTable = {}
        for line in data:gmatch("[^\r\n]+") do
            local cleanId = line:match("^%s*(.-)%s*$")
            if cleanId and cleanId ~= "" then
                table.insert(tempTable, cleanId)
            end
        end
        imageList = tempTable
    else

        imageList = {"rbxassetid://94766523133977"}
    end
end

updateImageList()

local function playEffect()
    if #imageList == 0 then return end
    local randomImage = imageList[math.random(1, #imageList)]
    local screenGui = Instance.new("ScreenGui")
    screenGui.Name = "KillEffectGui"
    screenGui.Parent = CoreGui
    screenGui.IgnoreGuiInset = true 
    local imageLabel = Instance.new("ImageLabel")
    imageLabel.Size = UDim2.new(1, 0, 1, 0)
    imageLabel.Position = UDim2.new(0.5, 0, 0.5, 0)
    imageLabel.AnchorPoint = Vector2.new(0.5, 0.5)
    imageLabel.BackgroundTransparency = 1
    imageLabel.Image = randomImage 
    imageLabel.Parent = screenGui
    local tween = TweenService:Create(imageLabel, TweenInfo.new(0.25, Enum.EasingStyle.Quart, Enum.EasingDirection.In), {
        Size = UDim2.new(0, 0, 0, 0),
        ImageTransparency = 1
    })
    task.wait(0.05)
    tween:Play()
    tween.Completed:Connect(function() screenGui:Destroy() end)
end

local function setupListener()
    local playerGui = player:WaitForChild("PlayerGui", 20)
    local mainGui = playerGui and playerGui:WaitForChild("MainGui", 20)
    if mainGui then
        local fighterInterfaces = mainGui:FindFirstChild("FighterInterfaces", true)
        if fighterInterfaces then
            local userFolder = fighterInterfaces:WaitForChild(player.Name, 20)
            local targetPath = userFolder and userFolder:WaitForChild("EliminationSlots", 20)
            if targetPath then
                targetPath.ChildAdded:Connect(function()
                    playEffect()
                end)
            else
            end
        else
        end
    else
    end
end

task.spawn(setupListener)
