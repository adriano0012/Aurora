local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local HttpService = game:GetService("HttpService")

local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera

local function GetRandomName()
    local chars = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ"
    local name = ""
    for i = 1, math.random(12, 25) do
        local r = math.random(1, #chars)
        name = name .. string.sub(chars, r, r)
    end
    return name
end

local RANDOM_GUI_NAME = GetRandomName()
local CONFIG_FILE = "system_settings_02.json"

local Settings = {
    Enabled = true,
    Radius = 20,
    OnlyHead = false,
    BlacklistedUsers = {},
    MenuOpen = false,
    MenuKey = Enum.KeyCode.P,
    ToggleKey = Enum.KeyCode.E,
    HeadKey = Enum.KeyCode.Z
}

local AllHitboxes = {
    "Head",
    "Left Arm",
    "Left Leg",
    "Right Arm",
    "Right Leg",
    "Torso"
}
local HeadHitboxes = {"Head"}

local function SaveData()
    local data = {
        Radius = Settings.Radius,
        BlacklistedUsers = Settings.BlacklistedUsers,
        OnlyHead = Settings.OnlyHead
    }
    if writefile then pcall(function() writefile(CONFIG_FILE, HttpService:JSONEncode(data)) end) end
end

if isfile and isfile(CONFIG_FILE) then
    local s, r = pcall(function() return HttpService:JSONDecode(readfile(CONFIG_FILE)) end)
    if s and r then
        Settings.Radius = r.Radius or 20
        Settings.BlacklistedUsers = r.BlacklistedUsers or {}
        Settings.OnlyHead = r.OnlyHead or false
    end
end

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = RANDOM_GUI_NAME
ScreenGui.ResetOnSpawn = false
if gethui then ScreenGui.Parent = gethui() else ScreenGui.Parent = game:GetService("CoreGui") end

local function CreateIndicator(pos)
    local dot = Instance.new("Frame", ScreenGui)
    dot.Name = GetRandomName()
    dot.Size = UDim2.new(0, 12, 0, 12)
    dot.Position = pos
    dot.BorderSizePixel = 0
    Instance.new("UICorner", dot)
    return dot
end

local StatusDot = CreateIndicator(UDim2.new(1, -40, 0, 10))
local HeadDot = CreateIndicator(UDim2.new(1, -20, 0, 10))

local function UpdateDots()
    StatusDot.BackgroundColor3 = Settings.Enabled and Color3.fromRGB(0, 134, 9) or Color3.fromRGB(206, 131, 188)
    HeadDot.BackgroundColor3 = Settings.OnlyHead and Color3.fromRGB(0, 150, 255) or Color3.fromRGB(80, 80, 80)
end
UpdateDots()

local MainFrame = Instance.new("Frame", ScreenGui)
MainFrame.Name = GetRandomName()
MainFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
MainFrame.Position = UDim2.new(1, -450, 0.5, -190)
MainFrame.Size = UDim2.new(0, 220, 0, 400)
MainFrame.BorderSizePixel = 0
MainFrame.Visible = false
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 8)

local dragging, dragInput, dragStart, startPos
MainFrame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true
        dragStart = input.Position
        startPos = MainFrame.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end
        end)
    end
end)

MainFrame.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement then
        dragInput = input
    end
end)

RunService.RenderStepped:Connect(function()
    if dragging and dragInput then
        local delta = dragInput.Position - dragStart
        MainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)

local Stroke = Instance.new("UIStroke", MainFrame)
Stroke.Thickness = 2
Stroke.Color = Color3.new(1, 1, 1)
Stroke.ApplyStrokeMode = "Border"

local Gradient = Instance.new("UIGradient", Stroke)
Gradient.Color = ColorSequence.new{
    ColorSequenceKeypoint.new(0, Color3.fromRGB(25, 25, 25)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(255, 162, 233))
}

task.spawn(function()
    local rot = 0
    RunService.RenderStepped:Connect(function(dt)
        rot = rot + (300 * dt)
        Gradient.Rotation = rot % 360
    end)
end)

local function AddImg(pos, id)
    local img = Instance.new("ImageLabel", MainFrame)
    img.BackgroundTransparency = 1
    img.Position = pos
    img.Size = UDim2.new(0, 30, 0, 30)
    img.Image = "rbxassetid://"..id
    return img
end
AddImg(UDim2.new(0.032, 3, 0.01, -2), "138895356591238")
AddImg(UDim2.new(0.827, 3, 0.01, -2), "138895356591238")

local Title = Instance.new("TextLabel", MainFrame)
Title.Size = UDim2.new(1, 0, 0, 40)
Title.BackgroundTransparency = 1
Title.Font = "GothamBold"
Title.Text = "1Qlua_Tiggеr"
Title.TextColor3 = Color3.fromRGB(200, 200, 200)
Title.TextSize = 16

local Divider = Instance.new("Frame", MainFrame)
Divider.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
Divider.BackgroundTransparency = 0.5
Divider.Position = UDim2.new(0.023, 6, 0, 36)
Divider.Size = UDim2.new(0.955, 0, 0, 2)
Divider.BorderSizePixel = 0

local function CreateBox(text, pos, size)
    local box = Instance.new("TextBox", MainFrame)
    box.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
    box.Position = pos
    box.Size = size
    box.Font = "GothamBold"
    box.TextColor3 = Color3.fromRGB(180, 180, 180)
    box.TextSize = 13
    box.PlaceholderText = text
    box.Text = ""
    Instance.new("UICorner", box).CornerRadius = UDim.new(0, 4)
    return box
end

local function CreateBtn(text, pos, size)
    local btn = Instance.new("TextButton", MainFrame)
    btn.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
    btn.Position = pos
    btn.Size = size
    btn.Font = "GothamBold"
    btn.TextColor3 = Color3.fromRGB(180, 180, 180)
    btn.TextSize = 13
    btn.Text = text
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 4)
    return btn
end

local RadiusInput = CreateBox("Radius: "..Settings.Radius, UDim2.new(0, 10, 0, 45), UDim2.new(0.527, -20, 0, 30))
local HeadToggle = CreateBtn("Head Only: "..(Settings.OnlyHead and "ON" or "OFF"), UDim2.new(0, 116, 0, 45), UDim2.new(0.518, -20, 0, 30))
local SearchInput = CreateBox("Search Players...", UDim2.new(0, 10, 0, 83), UDim2.new(1, -20, 0, 30))

RadiusInput.FocusLost:Connect(function()
    local val = tonumber(RadiusInput.Text)
    if val then 
        Settings.Radius = val 
        SaveData() 
    end
    RadiusInput.PlaceholderText = "Radius: "..Settings.Radius
    RadiusInput.Text = ""
end)

HeadToggle.MouseButton1Click:Connect(function()
    Settings.OnlyHead = not Settings.OnlyHead
    HeadToggle.Text = "Head Only: "..(Settings.OnlyHead and "ON" or "OFF")
    UpdateDots()
    SaveData()
end)

local Scroll = Instance.new("ScrollingFrame", MainFrame)
Scroll.BackgroundTransparency = 1
Scroll.Position = UDim2.new(0, 10, 0, 120)
Scroll.Size = UDim2.new(1, -20, 1, -130)
Scroll.ScrollBarThickness = 0
Instance.new("UIListLayout", Scroll).Padding = UDim.new(0, 5)

local function UpdateList(filter)
    for _, v in pairs(Scroll:GetChildren()) do if v:IsA("TextButton") then v:Destroy() end end
    
    for _, p in pairs(Players:GetPlayers()) do
        if p == LocalPlayer then continue end
        if filter and not p.Name:lower():find(filter:lower()) then continue end

        local Card = Instance.new("TextButton", Scroll)
        Card.Size = UDim2.new(1, 0, 0, 35)
        Card.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
        Card.Text = ""
        Instance.new("UICorner", Card)

        local Icon = Instance.new("ImageLabel", Card)
        Icon.Size = UDim2.new(0, 25, 0, 25)
        Icon.Position = UDim2.new(0, 5, 0, 5)
        task.spawn(function()
            local success, img = pcall(function() return Players:GetUserThumbnailAsync(p.UserId, Enum.ThumbnailType.HeadShot, Enum.ThumbnailSize.Size420x420) end)
            if success then Icon.Image = img end
        end)
        Instance.new("UICorner", Icon).CornerRadius = UDim.new(0, 5)

        local DName = Instance.new("TextLabel", Card)
        DName.Text = p.DisplayName
        DName.Position = UDim2.new(0.16, 0, 0.13, 0)
        DName.Size = UDim2.new(0.6, 0, 0, 20)
        DName.TextColor3 = Color3.fromRGB(200, 200, 200)
        DName.Font = "GothamBold"
        DName.TextSize = 14
        DName.TextXAlignment = "Left"
        DName.BackgroundTransparency = 1

        local Indicator = Instance.new("Frame", Card)
        Indicator.Size = UDim2.new(0, 7, 0, 20)
        Indicator.Position = UDim2.new(0.92, 0, 0.2, 0)
        local isOff = Settings.BlacklistedUsers[tostring(p.UserId)]
        Indicator.BackgroundColor3 = isOff and Color3.fromRGB(206, 131, 188) or Color3.fromRGB(0, 134, 9)
        Instance.new("UICorner", Indicator).CornerRadius = UDim.new(0, 5)

        Card.MouseButton1Click:Connect(function()
            local id = tostring(p.UserId)
            Settings.BlacklistedUsers[id] = not Settings.BlacklistedUsers[id] or nil
            Indicator.BackgroundColor3 = Settings.BlacklistedUsers[id] and Color3.fromRGB(206, 131, 188) or Color3.fromRGB(0, 134, 9)
            SaveData()
        end)
    end
end

SearchInput:GetPropertyChangedSignal("Text"):Connect(function() UpdateList(SearchInput.Text) end)

local function PerformClick(state)
    local m = UserInputService:GetMouseLocation()
    if mouse1press and mouse1release then
        if state then mouse1press() else mouse1release() end
    else
        local success, vim = pcall(function() return game:GetService("Virtual".."Input".."Manager") end)
        if success then vim:SendMouseButtonEvent(m.X, m.Y, 0, state, game, 1) end
    end
end

local function IsPlayerVisible(targetChar, targetPart)
    local origin = Camera.CFrame.Position
    local params = RaycastParams.new()
    params.FilterType = Enum.RaycastFilterType.Exclude
    params.FilterDescendantsInstances = {LocalPlayer.Character, Camera}

    local result = workspace:Raycast(origin, targetPart.Position - origin, params)
    return result and (result.Instance:IsDescendantOf(targetChar) or result.Instance == targetPart)
end

UserInputService.InputBegan:Connect(function(i, gp)
    if gp then return end
    if i.KeyCode == Settings.ToggleKey then
        Settings.Enabled = not Settings.Enabled
        UpdateDots()
        if not Settings.Enabled and MainFrame.Visible then
            MainFrame.Visible = false
            Settings.MenuOpen = false
        end
    elseif i.KeyCode == Settings.HeadKey then
        Settings.OnlyHead = not Settings.OnlyHead
        HeadToggle.Text = "Head Only: "..(Settings.OnlyHead and "ON" or "OFF")
        UpdateDots()
    elseif i.KeyCode == Settings.MenuKey then
        Settings.MenuOpen = not Settings.MenuOpen
        MainFrame.Visible = Settings.MenuOpen
        if Settings.MenuOpen then UpdateList() end
    end
end)

local isLeftClicking = false

RunService.RenderStepped:Connect(function()
    if not Settings.Enabled then
        if isLeftClicking then
            PerformClick(false)
            isLeftClicking = false
        end
        return
    end

    local center = Camera.ViewportSize / 2
    local activeHitboxes = Settings.OnlyHead and HeadHitboxes or AllHitboxes
    local targetFound = false

    for _, p in pairs(Players:GetPlayers()) do
        if p ~= LocalPlayer and p.Character and not Settings.BlacklistedUsers[tostring(p.UserId)] then
            -- Теперь ищем напрямую в p.Character, а не в папке Hitbox
            for _, name in pairs(activeHitboxes) do
                local part = p.Character:FindFirstChild(name)
                if part and part:IsA("BasePart") then
                    local screenPos, onScreen = Camera:WorldToViewportPoint(part.Position)
                    if onScreen then
                        local dist = (Vector2.new(screenPos.X, screenPos.Y) - center).Magnitude
                        if dist <= Settings.Radius and IsPlayerVisible(p.Character, part) then
                            targetFound = true
                            break
                        end
                    end
                end
            end
        end
        if targetFound then break end
    end

    if targetFound then
        if not isLeftClicking then
            isLeftClicking = true
            task.spawn(function()
                task.wait(math.random(15, 30) / 1000) 
                if targetFound then PerformClick(true) end
            end)
        end
    elseif isLeftClicking then
        PerformClick(false)
        isLeftClicking = false
    end
end)
